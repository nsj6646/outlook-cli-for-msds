# Domain Glossary (CONTEXT.md)

이 프로젝트는 다수의 고객사에게 맞춤형 물질안전정보자료(MSDS)를 대량으로 자동 배포하기 위한 시스템입니다.

## 핵심 도메인 용어

### 고객사 (Customer / Recipient)
- 물질안전정보자료(MSDS) 이메일을 수신하는 대상 업체 또는 담당자입니다.
- 이메일 주소(To), 고객사명, 담당자명 등의 고유 메타데이터를 가집니다.

### 제품코드 파일 접두사 (Product Code File Prefix)
- MSDS 폴더 내에서 해당 제품과 연계된 파일을 식별하기 위해 사용하는 파일명 접두사 규칙입니다.
- 파일명의 맨 첫부분이 반드시 `제품코드_` 형태로 언더바(_) 구분자를 포함하여 시작(대소문자 무관)해야만 일치하는 것으로 판단합니다. (예: 제품코드 P1003인 경우 `P1003_MSDS.pdf`는 매칭되지만, 다른 제품코드인 `P10030001` 등은 제외되어 겹침을 방지함)

### 제품 (Product)
- 고객사가 취급하거나 주문하는 화학 물질 또는 상품입니다.
- 고유한 제품코드와 제품명을 가집니다.
- 하나의 고객사는 여러 개의 제품을 취급할 수 있습니다.

### 물질안전정보자료 (MSDS / Material Safety Data Sheet)
- 각 제품의 안전한 취급 및 환경 위해성에 관한 정보가 기재된 문서(주로 PDF 파일)입니다.
- 제품과 1:1 또는 1:N으로 대응하며, 제품 개정 시 해당 제품을 취급하는 고객사들에게 최신 버전의 MSDS 파일이 배포되어야 합니다.
- **파일명 제약조건**: MSDS 파일들의 파일명은 제각각일 수 있으나, 항상 맨 앞부분(접두사)은 매핑할 대상 제품의 **제품코드**로 시작해야 합니다.

### 배포 (Distribution)
- 수신 조건에 맞는 고객사들에게 해당하는 제품 목록 표를 메일 본문에 삽입하고, 매핑된 MSDS 파일들을 첨부하여 아웃룩(Outlook)을 통해 대량 발송하는 프로세스입니다.

### 발신 계정 (Sender Account)
- 이메일을 보낼 때 사용하는 아웃룩에 등록된 개인 또는 회사 이메일 계정입니다.
- 엑셀 데이터의 지정을 통해 수신자별로 서로 다른 발신 계정을 동적으로 할당할 수 있습니다.

### 예약 발송 / 배달 지연 (Deferred Delivery)
- 이메일이 아웃룩에 의해 즉시 전송되지 않고, 지정된 미래의 특정 시간까지 전송을 지연(대기)시키는 기능입니다.
- 엑셀 데이터의 예약 시간을 기반으로 개별 메일에 예약 발송 설정이 가해집니다.

### 감사 로그 (Audit Log)
- 메일 배포 프로세스 진행 중 발생하는 모든 이벤트, 성공/실패 여부, 경고 문구(예: 특정 MSDS 파일 누락 등)를 영구 기록하는 파일입니다.
- 배포 담당자가 발송 결과 및 누락 건을 사후 검수하기 위한 목적으로 사용됩니다.

### 메일 배포 콘텍스트 (Mail Distribution Context)
- 엑셀 데이터 파일과 지정된 MSDS 폴더를 스캔하여 최종적으로 발송 가능한 이메일들의 명세 묶음(Mail Jobs)을 완성해내는 핵심 비즈니스 도메인 모듈입니다.
- 엑셀 파싱 및 제품코드 파일 접두사(Prefix) 매칭 등의 세부 비즈니스 로직을 하나로 통합(Collapse)하여 캡슐화한 개념입니다.

### 아웃룩 네이티브 표 (Outlook Native Table)
- 아웃룩 및 워드에서 기본적으로 삽입되는 검은색 얇은 실선 격자 테두리(MsoTableGrid) 형태의 표 스타일입니다.
- 테두리(`border: solid windowtext 1.0pt`)와 셀 여백(`padding: 0cm 5.4pt`) 구조를 가지며, 전체가 맑은 고딕(`11pt`) 글꼴로 표현됩니다.
