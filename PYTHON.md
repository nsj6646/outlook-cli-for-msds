# PYTHON.md

이 파일은 이 프로젝트에서 파이썬 환경 및 패키지를 관리하고 코드를 작성할 때 준수해야 하는 지침서입니다.

## 파이썬 개발 및 패키지 관리 규칙

1. **패키지 관리 도구 (`uv` 사용)**
   - 프로젝트의 모든 파이썬 라이브러리 설치, 환경 생성, 실행 등은 반드시 **`uv`** 도구를 사용합니다.
   - 글로벌 `pip` 또는 가상환경 내의 `pip install` 대신 **`uv pip install <package>`**를 사용하거나 **`uv run`**을 활용하여 패키지를 격리 및 실행합니다.
   - 패키지 의존성을 추가하거나 업데이트할 때는 반드시 `uv`를 사용하고, 필요시 가상환경(`venv`)을 생성할 때도 `uv venv`를 호출합니다.

2. **외부 라이브러리 검증**
   - 새로운 외부 파이썬 패키지(예: PDF 생성 라이브러리, 새로운 웹 라이브러리 등)를 도입하기 전에는 **Context7 MCP**를 활용하여 라이브러리의 사양, 보안 및 최신 공식 정보를 필히 확인합니다.

3. **코딩 스타일 및 아키텍처**
   - 가독성이 높은 구조화된 파이썬 코드를 작성하고, 함수의 입출력 타입을 명시하는 Type Hinting을 지향합니다.
   - 비동기 처리나 멀티스레딩이 동반되는 외부 COM 연동 시 스레드 모델 충돌을 미연에 방지하기 위해 `pythoncom`의 초기화(`CoInitialize`/`CoUninitialize`)를 철저히 처리합니다.
