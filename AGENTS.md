# AGENTS.md

이 파일은 이 프로젝트를 수행하는 모든 AI 에이전트가 반드시 준수해야 하는 지침서입니다.

## 에이전트 지침 규칙

1. **한국어 작성**
   - 모든 답변, 아티팩트, 문서는 반드시 **한국어**로 작성합니다.

2. **요구사항 확인**
   - 사용자 요구사항이 조금이라도 모호하거나 여러 방식으로 해석될 수 있는 경우, 임의로 판단하여 진행하지 말고 **반드시 사용자에게 확인 질문**을 하여 요구사항을 명확히 정의합니다.

3. **라이브러리 검증**
   - 새로운 외부 라이브러리나 기술을 도입 및 사용하기 전에는 **Context7 MCP**를 사용하여 해당 라이브러리의 최신 공식 정보와 변경점을 조회한 후 진행합니다.

4. **체계적 사고 및 계획**
   - 계획을 설계하거나 규모가 큰 작업을 착수할 때는 **sequential-thinking MCP**를 호출하여 단계별 분석 및 문제 해결 과정을 거칩니다.

5. **구현 계획 승인 대기**
   - 사용자가 구현 계획(Implementation Plan)을 승인하기 전에는 **절대** 코드 수정이나 실제 구현 작업을 시작하지 않으며, 반드시 사전 승인을 득한 후 실행합니다.
   - 단, 상위 에이전트(또는 사용자)가 전체 구현 계획을 이미 승인한 상태라면, 하위 서브 에이전트가 자체 수립하는 구현 계획은 자동 승인된 것으로 간주하여 추가적인 대기 없이 즉시 구현을 시작합니다.

6. **문서 최신성 유지**
   - 새로운 비즈니스 스펙이나 아키텍처 결정이 코드로 구현 완료되어 저장소에 반영될 때마다, 요구사항 정의서(`REQUIREMENTS.md`), 도메인 용어집(`CONTEXT.md`), 제품 기획서(`PRD.md`) 등 프로젝트 내의 모든 관련 기획/설계 문서를 **반드시 동시에 최신 상태로 업데이트**하여 실제 코드와의 완벽한 정합성을 상시 유지해야 합니다.
   - 프로젝트 정합성에 맞지 않는 과거 구버전 문서나 유휴(Dead) 리소스가 식별되면 방치하지 않고 즉시 청소 및 정리 계획을 세워 삭제합니다.

7. **Git을 통한 변경사항 관리**
   - 피처 개발, 리팩토링, 문서 최신화 등 작업 단위가 완료될 때마다 변경 이력을 형상 관리할 수 있도록 반드시 **Git**을 사용하여 변경 파일을 스테이징(`git add`)하고, 의미 있는 커밋 메시지와 함께 로컬 저장소에 커밋(`git commit`)하여 변경사항을 영구 보존해야 합니다.

## Agent skills

### Issue tracker

이 프로젝트의 이슈 및 PRD는 `.scratch/` 폴더 내에 마크다운 파일로 관리됩니다. 자세한 내용은 `docs/agents/issue-tracker.md` 파일을 참고하십시오.

### Triage labels

트리아지 상태 머신은 5개의 기본 레이블(`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`)을 그대로 사용합니다. 자세한 내용은 `docs/agents/triage-labels.md` 파일을 참고하십시오.

### Domain docs

이 코드베이스는 단일 컨텍스트 도메인 레이아웃을 사용하며, 루트 폴더의 `CONTEXT.md`를 기준으로 합니다. 자세한 내용은 `docs/agents/domain.md` 파일을 참고하십시오.
