# 프로젝트 요구사항 정의서 (REQUIREMENTS.md)

이 파일은 프로젝트의 추가 요구사항을 정의하고 추적(trace)하기 위한 최종 명세서입니다. 사용자와의 그릴링 설계 인터뷰 및 개발 결과를 바탕으로 최신화되었습니다.

## 1. 개요 및 최종 목표
- **목표**: 다수의 고객사(대량 발송)에게 각 고객사별로 맞춤형 첨부파일 및 정보를 포함한 메일을 자동으로 배포하는 시스템 구축.

## 2. 핵심 기능 요구사항

### 2.1. 고객사 맞춤형 파일 첨부 및 배포
- **제품코드 접두사(Prefix) 매칭 자동 첨부**: 
  - 각 고객사가 취급하는 제품군(제품코드)에 매핑되는 서류들을 MSDS 보관 폴더에서 스캔하여 메일에 동적으로 다중 첨부합니다.
  - 파일명의 첫부분이 반드시 `제품코드_` 형태로 언더바(_) 구분자를 붙여 시작(대소문자 무관)하는 파일들만 스캔해 수집하여, 제품코드 간의 글자 겹침에 의한 오첨부(예: P1003 제품 메일에 P10030001 파일이 들어가는 버그)를 방지합니다.

### 2.2. 동적 메일 본문 및 정보 생성
- **제품 정보 표 삽입 (아웃룩 순정 격자 표 양식)**:
  - 각 고객사가 취급하는 제품 리스트를 메일 본문에 표(Table) 형태로 동적으로 구성해 삽입합니다.
  - 표의 스타일은 아웃룩/워드 순정 삽입 격자 표(`MsoTableGrid`) 양식을 그대로 재현합니다. 얇은 검정 격자 테두리(`border: solid windowtext 1.0pt`)와 셀 패딩(`padding: 0cm 5.4pt`) 구조를 지니며, 헤더 배경은 투명합니다.

### 2.3. 메일 발송 템플릿 및 폰트 규격
- **메일 정보**:
  - **제목**: `[Sun Chemical Korea] 물질안정정보자료 개정에 따른 제공건 - "고객사 명"`
  - **수신 (To)**: `"고객사 명"`, `"담당자명"` 귀하
  - **참조 (Cc)**: 엑셀 `Recipients` 시트의 `Cc` 컬럼에 기재된 수신처 주소들을 바인딩합니다. 여러 명일 경우 세미콜론 `;` 구분자를 지원하여 아웃룩 참조 필드에 다중 등록합니다.
  - **기본 글꼴 서식 강제화**:
    - 메일 전체 본문과 표 글꼴에 맑은 고딕(`맑은 고딕`, `Malgun Gothic`, sans-serif) 11.0pt 크기를 강제 적용하여 시각적 일질감을 완전히 해소합니다.
  - **본문 내용**: 
    - 직접 작성 영역 (기본 인사말 및 안내 문구)
    - 취급 제품 정보 테이블 예시:
      | 번호 | 제품코드 | 제품명 |
      | :--- | :--- | :--- |
      | 1 | P1001 | 알파 |
      | 2 | P1002 | 베타 |
      | 3 | P1003 | 감마 |

### 2.4. 예외 정책 및 자가 정리 감사 로그
- **예외 복원력**:
  - 엑셀 파일 누락과 같은 구조적 오류 발생 시에는 프로그램을 즉시 중단(ValueError 전파)합니다.
  - 특정 수신자의 MSDS 서류가 폴더에 누락되었거나 예약 발송 시간 포맷이 깨진 경우 등의 개별 오류는, 해당 행 수신인만 건너뛰고(Skip) 경고 감사 로그를 기록한 뒤 정상 데이터만 모아 메일을 일괄 작성합니다.
- **감사 로그(Audit Log) 및 보존 정리 정책**:
  - 실행 시마다 타임스탬프 기반 개별 로그 파일(`logs/distribution_YYYYMMDD_HHMMSS.log`)을 생성하여 세션별 히스토리를 증적 관리합니다.
  - 디스크 오염을 방지하기 위해 프로그램 실행 시점에 `logs/` 디렉토리 내에서 수정 일자 기준 **7일 보존 기한이 경과한 오래된 로그 파일들을 자동으로 감지하여 청소(Auto-cleanup)**합니다.

## 3. 비기능 요구사항
- **데이터 소스 연동**: 
  - `Recipients` 시트(To, Cc, Subject, Body, From, 예약시간 컬럼)와 제품군이 매핑된 `TableData` 시트(Email, 번호, 제품코드, 제품명 컬럼)의 2개 시트로 구성된 단일 엑셀 파일([test_recipients_v4.xlsx](file:///C:/Users/seungjoo.na/Documents/antigravity/clever-darwin/test_recipients_v4.xlsx))을 데이터베이스로 활용합니다.
- **백그라운드 초안 일괄 저장**:
  - 대량 메일 구동 시 아웃룩 이메일 창이 개별 팝업되어 화면을 방해하지 않도록, `mail.Save()` API를 활용하여 아웃룩 **임시 보관함(Drafts)**에 조용히 백그라운드로 저장합니다.
